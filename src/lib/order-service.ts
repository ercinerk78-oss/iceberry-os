import{prisma}from"@/lib/prisma";import{accountingService}from"@/lib/accounting";import{orderNumber}from"@/lib/warehouse";import{orderSchema}from"@/lib/validations/order";
type Input=Parameters<typeof orderSchema.parse>[0];
export async function createOrder(input:Input){const data=orderSchema.parse(input);return prisma.$transaction(async tx=>{const products=await tx.product.findMany({where:{id:{in:data.items.map(i=>i.productId)},isActive:true,archivedAt:null}});if(products.length!==data.items.length)throw new Error("Sepette artık satışta olmayan ürün var.");const stocks=await tx.warehouseStock.findMany({where:{warehouseId:data.warehouseId,productId:{in:products.map(p=>p.id)}}});for(const item of data.items){const stock=stocks.find(s=>s.productId===item.productId);if(!stock||stock.availableQuantity<item.quantity)throw new Error(`${products.find(p=>p.id===item.productId)?.name} için yeterli kullanılabilir stok yok.`)}const lines=data.items.map(item=>{const p=products.find(x=>x.id===item.productId)!;const sub=p.salePrice*item.quantity,vat=sub*p.vatRate/100;return{productId:p.id,productName:p.name,sku:p.sku,unit:p.unit,quantity:item.quantity,unitPrice:p.salePrice,vatRate:p.vatRate,lineSubtotal:sub,lineVat:vat,lineTotal:sub+vat}});const subtotal=lines.reduce((a,b)=>a+b.lineSubtotal,0),vatTotal=lines.reduce((a,b)=>a+b.lineVat,0);return tx.franchiseOrder.create({data:{orderNumber:orderNumber(),franchiseeId:data.franchiseeId,branchId:data.branchId||null,warehouseId:data.warehouseId,subtotal,vatTotal,grandTotal:subtotal+vatTotal,requestedDeliveryDate:data.requestedDeliveryDate?new Date(data.requestedDeliveryDate):null,notes:data.notes,createdBy:"Iceberry OS",items:{create:lines},activities:{create:{type:"ORDER_CREATED",description:"Sipariş incelemeye gönderildi.",createdBy:"Iceberry OS"}}}})})}
export async function approveOrder(id:string){return prisma.$transaction(async tx=>{const order=await tx.franchiseOrder.findUnique({where:{id},include:{items:true}});if(!order)throw new Error("Sipariş bulunamadı.");if(order.status!=="SUBMITTED")return order;for(const item of order.items){const stock=await tx.warehouseStock.findUnique({where:{warehouseId_productId:{warehouseId:order.warehouseId,productId:item.productId}}});if(!stock||stock.availableQuantity<item.quantity)throw new Error(`${item.productName} için yeterli stok yok.`);await tx.warehouseStock.update({where:{id:stock.id},data:{reservedQuantity:{increment:item.quantity},availableQuantity:{decrement:item.quantity}}});await tx.stockMovement.create({data:{warehouseId:order.warehouseId,productId:item.productId,movementType:"RESERVE",quantity:item.quantity,beforeQuantity:stock.availableQuantity,afterQuantity:stock.availableQuantity-item.quantity,referenceType:"ORDER",referenceId:id,description:`${order.orderNumber} için rezerve edildi.`}})}return tx.franchiseOrder.update({where:{id},data:{status:"APPROVED",approvedAt:new Date(),approvedBy:"Iceberry OS",activities:{create:{type:"STATUS_CHANGED",description:"Sipariş onaylandı ve stok rezerve edildi."}}}})})}
export async function createInvoice(id:string){const order=await prisma.franchiseOrder.findUnique({where:{id},include:{franchisee:true,items:true}});if(!order)throw new Error("Sipariş bulunamadı.");if(order.parasutInvoiceId)return order;const accounting=accountingService();await accounting.findOrCreateContact(order.franchisee.companyName,order.franchisee.taxNumber);for(const item of order.items)await accounting.findOrCreateProduct({name:item.productName,sku:item.sku});const invoice=await accounting.createSalesInvoice({orderId:id,orderNumber:order.orderNumber,contactName:order.franchisee.companyName,total:order.grandTotal,currency:order.currency});return prisma.franchiseOrder.update({where:{id},data:{invoiceStatus:"CREATED",parasutInvoiceId:invoice.id,parasutInvoiceNumber:invoice.number,activities:{create:{type:"INVOICE_CREATED",description:`Mock satış faturası oluşturuldu: ${invoice.number}`}}}})}
export async function changeOrderStatus(id:string,status:string){return prisma.franchiseOrder.update({where:{id},data:{status,activities:{create:{type:"STATUS_CHANGED",description:`Sipariş durumu ${status} olarak güncellendi.`}}}})}
export async function releaseOrder(id:string,status:"REJECTED"|"CANCELLED"){return prisma.$transaction(async tx=>{const order=await tx.franchiseOrder.findUnique({where:{id},include:{items:true}});if(!order)throw new Error("Sipariş bulunamadı.");if(["REJECTED","CANCELLED","SHIPPED","DELIVERED"].includes(order.status))return order;if(["APPROVED","WAREHOUSE_QUEUE","PREPARING","READY"].includes(order.status))for(const item of order.items){const stock=await tx.warehouseStock.findUnique({where:{warehouseId_productId:{warehouseId:order.warehouseId,productId:item.productId}}});if(stock){const released=Math.min(stock.reservedQuantity,item.quantity);await tx.warehouseStock.update({where:{id:stock.id},data:{reservedQuantity:{decrement:released},availableQuantity:{increment:released}}});await tx.stockMovement.create({data:{warehouseId:order.warehouseId,productId:item.productId,movementType:"RELEASE",quantity:released,beforeQuantity:stock.availableQuantity,afterQuantity:stock.availableQuantity+released,referenceType:"ORDER",referenceId:id,description:"Sipariş kapatıldığı için rezervasyon iade edildi."}})}}return tx.franchiseOrder.update({where:{id},data:{status,[status==="REJECTED"?"rejectedAt":"cancelledAt"]:new Date(),activities:{create:{type:"STATUS_CHANGED",description:status==="REJECTED"?"Sipariş reddedildi.":"Sipariş iptal edildi."}}}})})}
export async function prepareOrder(id:string,items:{id:string;preparedQuantity:number;missingQuantity:number}[]){return prisma.$transaction(async tx=>{for(const item of items)await tx.franchiseOrderItem.update({where:{id:item.id},data:{preparedQuantity:item.preparedQuantity,missingQuantity:item.missingQuantity}});return tx.franchiseOrder.update({where:{id},data:{status:"PREPARING",activities:{create:{type:"PREPARATION",description:"Depo hazırlık miktarları güncellendi."}}}})})}
export async function shipOrder(id:string,carrierName?:string,trackingNumber?:string){return prisma.$transaction(async tx=>{const order=await tx.franchiseOrder.findUnique({where:{id},include:{items:true,shipment:true}});if(!order)throw new Error("Sipariş bulunamadı.");if(order.status==="SHIPPED")return order;if(order.invoiceStatus!=="CREATED")throw new Error("Fatura oluşmadan sevkiyat yapılamaz.");for(const item of order.items){if(item.preparedQuantity+item.missingQuantity<item.quantity)throw new Error("Tüm kalemler kontrol edilmeden sevkiyat yapılamaz.");if(item.missingQuantity>0)throw new Error("Eksik ürün bulunan sipariş sevk edilemez.");const stock=await tx.warehouseStock.findUnique({where:{warehouseId_productId:{warehouseId:order.warehouseId,productId:item.productId}}});if(!stock||stock.quantity<item.quantity)throw new Error("Fiziksel stok yetersiz.");await tx.warehouseStock.update({where:{id:stock.id},data:{quantity:{decrement:item.quantity},reservedQuantity:{decrement:item.quantity}}});await tx.stockMovement.create({data:{warehouseId:order.warehouseId,productId:item.productId,movementType:"OUT",quantity:item.quantity,beforeQuantity:stock.quantity,afterQuantity:stock.quantity-item.quantity,referenceType:"SHIPMENT",referenceId:id,description:`${order.orderNumber} sevkiyatı.`}})}await tx.shipment.upsert({where:{orderId:id},create:{orderId:id,warehouseId:order.warehouseId,shipmentNumber:`SVK-${order.orderNumber}`,status:"SHIPPED",carrierName,trackingNumber,shippedAt:new Date()},update:{status:"SHIPPED",carrierName,trackingNumber,shippedAt:new Date()}});return tx.franchiseOrder.update({where:{id},data:{status:"SHIPPED",activities:{create:{type:"SHIPPED",description:"Sipariş sevk edildi ve stoktan düşüldü."}}}})})}
