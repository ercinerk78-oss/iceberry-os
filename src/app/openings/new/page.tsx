import{AppShell}from"@/components/app-shell";import{OpeningForm}from"@/components/openings/opening-form";import{Card,CardContent}from"@/components/ui/card";import{prisma}from"@/lib/prisma";export const dynamic="force-dynamic";
export default async function NewOpening(){const branches=await prisma.branch.findMany({where:{archivedAt:null,status:{in:["PLANNED","IN_SETUP","CONTRACTED"]},openings:{none:{archivedAt:null,status:{notIn:["COMPLETED","CANCELLED"]}}}},select:{id:true,branchName:true,branchCode:true,city:true},orderBy:{branchName:"asc"}});return <AppShell activeHref="/openings" eyebrow="Yeni proje" title="Yeni Açılış Projesi"><Card className="shadow-none"><CardContent className="p-5">{branches.length?<OpeningForm branches={branches}/>:<p className="p-10 text-center text-[#65705f]">Aktif projesi olmayan planlanmış veya kurulumdaki şube bulunamadı.</p>}</CardContent></Card></AppShell>}
