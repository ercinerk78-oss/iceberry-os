import{archiveUser,createUser,resetPassword,toggleUser,updateUser}from"@/app/settings/users/actions";import{AppShell}from"@/components/app-shell";import{Button}from"@/components/ui/button";import{Card,CardContent,CardHeader,CardTitle}from"@/components/ui/card";import{requirePermission}from"@/lib/auth";import{ROLE_LABELS,USER_ROLES}from"@/lib/permissions";import{prisma}from"@/lib/prisma";export const dynamic="force-dynamic";const input="h-10 rounded-lg border px-3";
export default async function UsersPage(){await requirePermission("users");const users=await prisma.user.findMany({orderBy:{createdAt:"desc"}});const date=(d:Date|null)=>d?new Intl.DateTimeFormat("tr-TR",{dateStyle:"short",timeStyle:"short"}).format(d):"—";return <AppShell activeHref="/settings/users" eyebrow="Sistem ayarları" title="Kullanıcı Yönetimi"><div className="space-y-5"><Card className="shadow-none"><CardHeader><CardTitle>Yeni Kullanıcı</CardTitle></CardHeader><CardContent><form action={createUser} className="grid gap-3 md:grid-cols-2 xl:grid-cols-5"><input name="name" placeholder="Ad Soyad" className={input} required/><input name="email" type="email" placeholder="E-posta" className={input} required/><input name="phone" placeholder="Telefon" className={input}/><select name="role" className={input}>{USER_ROLES.map(r=><option key={r} value={r}>{ROLE_LABELS[r]}</option>)}</select><input name="password" type="password" placeholder="Geçici şifre" className={input} required/><Button className="md:col-span-2 xl:col-span-5">Kullanıcı Oluştur</Button></form></CardContent></Card><div className="space-y-3">{users.map(u=><Card key={u.id} className={`shadow-none ${u.archivedAt?"opacity-60":""}`}><CardContent className="p-4"><form action={updateUser.bind(null,u.id)} className="grid gap-3 lg:grid-cols-[1fr_1fr_160px_190px_auto]"><input name="name" defaultValue={u.name} className={input}/><input name="email" type="email" defaultValue={u.email} className={input}/><input name="phone" defaultValue={u.phone??""} className={input}/><select name="role" defaultValue={u.role} className={input}>{USER_ROLES.map(r=><option key={r} value={r}>{ROLE_LABELS[r]}</option>)}</select><input name="password" type="hidden" value=""/><Button variant="outline">Kaydet</Button></form><div className="mt-3 flex flex-wrap items-center gap-2 text-xs text-[#65705f]"><span>{u.isActive?"Aktif":"Pasif"}</span><span>· Son giriş {date(u.lastLoginAt)}</span><span>· Oluşturma {date(u.createdAt)}</span>{!u.archivedAt&&<><form action={toggleUser.bind(null,u.id)}><Button size="sm" variant="outline">{u.isActive?"Pasifleştir":"Aktifleştir"}</Button></form><form action={resetPassword.bind(null,u.id)} className="flex gap-1"><input name="password" defaultValue="Iceberry123!" className="h-7 rounded border px-2"/><Button size="sm" variant="outline">Şifreyi Sıfırla</Button></form><form action={archiveUser.bind(null,u.id)}><Button size="sm" variant="destructive">Arşivle</Button></form></>}</div></CardContent></Card>)}</div></div></AppShell>}
