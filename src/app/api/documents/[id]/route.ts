import{NextRequest}from"next/server";import{requirePermission}from"@/lib/auth";import{prisma}from"@/lib/prisma";import{storage}from"@/lib/storage";export const runtime="nodejs";
export async function GET(request:NextRequest,{params}:{params:Promise<{id:string}>}){await requirePermission("documents");const{id}=await params,document=await prisma.document.findUnique({where:{id}});if(!document)return new Response("Dosya bulunamadı.",{status:404});try{const data=await storage.read(document.filePath),download=request.nextUrl.searchParams.get("download")==="1",safeName=document.originalFileName.replace(/[\r\n"]/g,"_");return new Response(new Uint8Array(data),{headers:{"Content-Type":document.mimeType,"Content-Length":String(document.fileSize),"Content-Disposition":`${download?"attachment":"inline"}; filename="${safeName}"; filename*=UTF-8''${encodeURIComponent(document.originalFileName)}`,"Cache-Control":"private, no-store"}})}catch{return new Response("Fiziksel dosya bulunamadı.",{status:404})}}
