import{AppShell}from"@/components/app-shell";import{OrderTable}from"@/components/orders/order-table";import{prisma}from"@/lib/prisma";import{ORDER_STATUSES,warehouseLabel}from"@/lib/warehouse";export const dynamic="force-dynamic";
export default async function AdminOrders({searchParams}:{searchParams:Promise<{status?:string;q?:string}>}){const s=await searchParams;const orders=await prisma.franchiseOrder.findMany({where:{status:s.status||undefined,OR:s.q?[{orderNumber:{contains:s.q}},{franchisee:{companyName:{contains:s.q}}}]:undefined},include:{franchisee:true,branch:true,items:true},orderBy:{createdAt:"desc"}});return <AppShell activeHref="/orders/admin" eyebrow="Merkez sipariş yönetimi" title="Siparişler"><div className="space-y-4"><form className="flex flex-wrap gap-2 rounded-xl border bg-white p-4"><input name="q" defaultValue={s.q} placeholder="Sipariş no veya bayi ara" className="h-10 min-w-64 rounded-lg border px-3"/><select name="status" defaultValue={s.status} className="h-10 rounded-lg border px-3"><option value="">Tüm durumlar</option>{ORDER_STATUSES.map(([v])=><option key={v} value={v}>{warehouseLabel(ORDER_STATUSES,v)}</option>)}</select><button className="rounded-lg bg-[#17201b] px-4 text-white">Filtrele</button></form><OrderTable orders={orders} admin/></div></AppShell>}

