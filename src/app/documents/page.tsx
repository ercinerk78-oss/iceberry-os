import Link from"next/link";import type{Prisma}from"@prisma/client";import{Archive,Download,Eye,FileText,RotateCcw,Search}from"lucide-react";import{archiveDocument,restoreDocument}from"@/app/documents/actions";import{AppShell}from"@/components/app-shell";import{Badge}from"@/components/ui/badge";import{Button}from"@/components/ui/button";import{Card}from"@/components/ui/card";import{DOCUMENT_TYPES,documentTypeLabel,formatFileSize}from"@/lib/documents";import{prisma}from"@/lib/prisma";
export const dynamic="force-dynamic";type Params=Record<string,string|string[]|undefined>;const get=(p:Params,k:string)=>typeof p[k]==="string"?p[k]as string:"";
export default async function DocumentsPage({searchParams}:{searchParams:Promise<Params>}){const p=await searchParams,q=get(p,"q"),type=get(p,"type"),relation=get(p,"relation"),shared=get(p,"shared"),archive=get(p,"archive"),date=get(p,"date");const where:Prisma.DocumentWhereInput={};if(q)where.OR=[{originalFileName:{contains:q}},{candidate:{fullName:{contains:q}}},{branch:{branchName:{contains:q}}},{locationId:{contains:q}}];if(type)where.documentType=type;if(relation==="candidate")where.candidateId={not:null};if(relation==="branch")where.branchId={not:null};if(relation==="location")where.locationId={not:null};if(relation==="unlinked")where.AND=[{candidateId:null},{branchId:null},{locationId:null}];if(shared==="yes")where.customerShared=true;if(shared==="no")where.customerShared=false;if(archive==="active")where.archivedAt=null;if(archive==="archived")where.archivedAt={not:null};if(date)where.uploadedAt={gte:new Date(`${date}T00:00:00`),lte:new Date(`${date}T23:59:59.999`)};const items=await prisma.document.findMany({where,include:{candidate:{select:{id:true,fullName:true}},franchisee:{select:{id:true,companyName:true}},branch:{select:{id:true,branchName:true}}},orderBy:{uploadedAt:"desc"}});return <AppShell activeHref="/documents" eyebrow="Operasyon arşivi" title="Dokümanlar"><div className="space-y-4"><Card className="p-4 shadow-none"><form className="grid gap-3 md:grid-cols-3 xl:grid-cols-7"><label className="relative xl:col-span-2"><Search className="absolute left-3 top-3 size-4"/><input name="q" defaultValue={q} placeholder="Dosya, aday, lokasyon veya şube" className="h-10 w-full rounded-lg border pl-9 pr-3"/></label><Sel name="type" current={type} first="Tüm doküman türleri" options={Object.entries(DOCUMENT_TYPES)}/><Sel name="relation" current={relation} first="Tüm bağlı kayıtlar" options={[["candidate","Aday"],["branch","Şube"],["location","Lokasyon"],["unlinked","Bağlantısız"]]}/><Sel name="shared" current={shared} first="Tüm paylaşım durumları" options={[["yes","Paylaşıldı"],["no","Paylaşılmadı"]]}/><Sel name="archive" current={archive} first="Tüm arşiv durumları" options={[["active","Aktif"],["archived","Arşivlenmiş"]]}/><input type="date" name="date" defaultValue={date} aria-label="Yükleme tarihi" className="h-10 rounded-lg border px-3"/><div className="flex gap-2"><Button>Filtrele</Button><Button asChild type="button" variant="outline"><Link href="/documents">Temizle</Link></Button></div></form></Card><Card className="overflow-hidden shadow-none"><div className="overflow-x-auto"><table className="w-full min-w-[1050px] text-left text-sm"><thead className="bg-[#f8faf6] text-xs uppercase text-[#65705f]"><tr>{["Doküman Adı","Tür","Bağlı Kayıt","Versiyon","Yükleme Tarihi","Paylaşım","İşlemler"].map(h=><th key={h} className="px-4 py-3">{h}</th>)}</tr></thead><tbody className="divide-y">{items.map(d=><tr key={d.id}><td className="px-4 py-4"><div className="flex gap-2"><FileText className="size-4"/><div><p className="max-w-52 truncate font-semibold">{d.originalFileName}</p><p className="text-xs text-[#65705f]">{formatFileSize(d.fileSize)}{d.archivedAt?" · Arşivde":""}</p></div></div></td><td className="px-4 py-4">{documentTypeLabel(d.documentType)}</td><td className="px-4 py-4"><Linked document={d}/></td><td className="px-4 py-4"><Badge variant="outline">v{d.version}</Badge></td><td className="px-4 py-4">{new Intl.DateTimeFormat("tr-TR",{dateStyle:"medium",timeStyle:"short"}).format(d.uploadedAt)}</td><td className="px-4 py-4"><Badge className={d.customerShared?"bg-emerald-100 text-emerald-800":"bg-amber-100 text-amber-800"}>{d.customerShared?"Paylaşıldı":"Paylaşılmadı"}</Badge></td><td className="px-4 py-4"><div className="flex gap-2"><Button asChild size="sm" variant="outline"><a href={`/api/documents/${d.id}`} target="_blank"><Eye/>Görüntüle</a></Button><Button asChild size="sm" variant="outline"><a href={`/api/documents/${d.id}?download=1`}><Download/>İndir</a></Button><form action={(d.archivedAt?restoreDocument:archiveDocument).bind(null,d.id)}><Button size="sm" variant="outline">{d.archivedAt?<RotateCcw/>:<Archive/>}{d.archivedAt?"Geri Al":"Arşivle"}</Button></form></div></td></tr>)}{!items.length&&<tr><td colSpan={7} className="p-14 text-center text-[#65705f]">Filtrelere uygun doküman bulunamadı.</td></tr>}</tbody></table></div></Card></div></AppShell>}
type LinkedProps={document:{candidate:{id:string;fullName:string}|null;franchisee:{id:string;companyName:string}|null;branch:{id:string;branchName:string}|null;locationId:string|null}};function Linked({document:d}:LinkedProps){if(d.candidate)return <Link href={`/candidates/${d.candidate.id}`} className="font-medium underline">{d.candidate.fullName}</Link>;if(d.branch)return <Link href={`/branches/${d.branch.id}`} className="font-medium underline">{d.branch.branchName}</Link>;if(d.franchisee)return <span>{d.franchisee.companyName}</span>;return d.locationId?`Lokasyon: ${d.locationId}`:"Bağlantısız"}
function Sel({name,current,first,options}:{name:string;current:string;first:string;options:string[][]}){return <select name={name} defaultValue={current} aria-label={first} className="h-10 rounded-lg border px-3"><option value="">{first}</option>{options.map(([v,l])=><option key={v} value={v}>{l}</option>)}</select>}
