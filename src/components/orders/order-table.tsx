import Link from"next/link";import{orderCommand}from"@/app/orders/actions";import{Badge}from"@/components/ui/badge";import{Button}from"@/components/ui/button";import{dateTime,money,ORDER_STATUSES,warehouseLabel}from"@/lib/warehouse";
type Row={id:string;orderNumber:string;status:string;invoiceStatus:string;grandTotal:number;currency:string;createdAt:Date;franchisee:{companyName:string};branch:{branchName:string}|null;items:{quantity:number}[]};
export function OrderTable({orders,admin=false}:{orders:Row[];admin?:boolean}){return <div className="overflow-x-auto rounded-xl border bg-white"><table className="w-full min-w-[850px] text-sm"><thead className="bg-[#f1f4ef] text-left"><tr>{["Sipariş","Bayi / Şube","Durum","Kalem","Tutar","Tarih","İşlem"].map(x=><th key={x} className="p-3">{x}</th>)}</tr></thead><tbody>{orders.map(o=><tr key={o.id} className="border-t"><td className="p-3 font-semibold">{o.orderNumber}</td><td className="p-3">{o.franchisee.companyName}<div className="text-xs text-[#65705f]">{o.branch?.branchName??"Genel"}</div></td><td className="p-3"><Badge variant="outline">{warehouseLabel(ORDER_STATUSES,o.status)}</Badge><div className="mt-1 text-xs">Fatura: {o.invoiceStatus}</div></td><td className="p-3">{o.items.length} / {o.items.reduce((a,b)=>a+b.quantity,0)} ürün</td><td className="p-3">{money(o.grandTotal,o.currency)}</td><td className="p-3">{dateTime(o.createdAt)}</td><td className="p-3"><div className="flex gap-1"><Button asChild size="sm" variant="outline"><Link href={admin?`/orders/admin/${o.id}`:`/warehouse/orders/${o.id}`}>İncele</Link></Button>{admin&&o.status==="SUBMITTED"&&<form action={orderCommand.bind(null,o.id,"approve")}><Button size="sm">Onayla</Button></form>}</div></td></tr>)}{!orders.length&&<tr><td colSpan={7} className="p-10 text-center text-[#65705f]">Kayıt bulunamadı.</td></tr>}</tbody></table></div>}
